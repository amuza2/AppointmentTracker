[project]
name = "house-tracker"
version = "0.1.0"
description = "Track house viewing appointments with filtering, archiving, and reminders"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "flet>=0.85.3",
    "psycopg2-binary>=2.9.12",
]
