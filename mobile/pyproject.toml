[project]
name = "house-tracker-mobile"
version = "0.1.0"
description = "House Tracker mobile app - calls the API backend"
requires-python = ">=3.12"
dependencies = [
    "flet>=0.85.3",
    "httpx>=0.28.1",
]
